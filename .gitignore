# Rust build artifacts
/target

# Runtime state (realtime engine persisted state)
/data/

# Backup & Restore runtime config (machine-specific backup path/schedule)
backup_config.json

# OS / editor junk
.DS_Store
*.swp
